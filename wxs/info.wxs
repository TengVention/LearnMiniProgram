// JS代码
var message = 'helloworld';
var name = 'tengvention';
var sum = function (num1, num2) {
  return num1 + num2
};

// //commonjs的模块化导出的写法
module.exports = {
  message: message,
  name: name,
  sum: sum
}